<%= semantic_form_for [@post, @comment || Comment.new] do |f| %>
  <%= f.inputs do %>
		<%= f.input :name %>
		<%= f.input :email %>
		<%= f.input :url, :label => "URL" %>
    <%= f.input :comment %>
  <% end %>

  <%= f.buttons %>
<% end %>
