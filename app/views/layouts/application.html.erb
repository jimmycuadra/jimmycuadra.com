<!DOCTYPE html>
<html>
<head>
  <title><%= "#{@page_title} | " if @page_title.present? %><%= "Page #{params[:page]} | " if params[:page] %>Jimmy Cuadra</title>
  <%= stylesheet_link_tag 'application' %>
  <%= csrf_meta_tags %>
  <link rel="alternate" type="application/rss+xml" title="RSS" href="http://feeds.feedburner.com/jimmycuadra" />
  <%= render :partial => "partials/production_scripts" if Rails.env.production? %>
</head>
<body>
  <%= render :partial => "partials/site_header" %>

  <div class="content">
    <div class="bucket">
      <nav>
        <ul>
          <li><%= link_to_unless_current "Blog", root_path %></li>
          <li><%= link_to_unless_current "About", about_path %></li>
          <li><%= link_to_unless_current "Projects", projects_path %></li>
        </ul>
      </nav>

      <% if flash[:notice] %>
        <section class="notice" id="flash"><%= flash[:notice] %></section>
      <% end %>

      <%= render :partial => "partials/admin_actions" if admin? %>

      <%= yield %>
    </div>
  </div>

  <%= render :partial => "partials/site_footer" %>

  <%= javascript_include_tag "jquery.min" unless Rails.env.production? %>
  <%= javascript_include_tag "application" %>
  <%= render :partial => "partials/social_scripts" %>
</body>
</html>
